	<!-- Content -->
	<div class="content">
		<div class="container">
			<!-- Post -->
			<div class="post">
				<!-- Heading -->				
                <h2><%= @article.title %></h2>
				<hr>
				<div class="in-content">
					<p><%= @article.text %></p>
				</div>
            </div>
			<!-- /post -->

<h2>Comments</h2>
<%= render @article.comments %>            
            
<h2>Add a comment: (requires login)</h2>
<%= render 'comments/form' %>
                        
    <%= link_to 'Edit', edit_article_path(@article), class: "btn-yellow btn-lg btn-primary" %> |            
    <%= link_to 'Back', articles_path, class: "btn-yellow btn-lg btn-primary" %>
        </div>
	</div>
